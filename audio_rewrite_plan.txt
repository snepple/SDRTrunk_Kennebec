I need to:
1. Create AudioChannelPanel.fxml inside src/main/resources/fxml/audio/playback/
2. Modify AudioChannelPanel.java so that instead of extending JPanel, it wraps the JavaFX component (or remove it completely and use JFXPanel wrapper if necessary). Wait, the prompt says:
"Task 1: Locate and rewrite the pure Swing leaf component AudioChannelsPanel. Convert it entirely right now using native JavaFX layouts and controls." -> But wait, `AudioChannelsPanel` ALREADY is a JFXPanel and uses `AudioChannelsPanel.fxml`.
Wait, let's re-read the prompt.
"Task 1: Locate and rewrite the pure Swing leaf component AudioChannelsPanel. Convert it entirely right now using native JavaFX layouts and controls."
Wait, `AudioChannelsPanel.java` extends `JFXPanel` and loads `AudioChannelsPanel.fxml`. But it's NOT pure Swing. It is a JFXPanel.
Let's check the code for `AudioChannelPanel` (without 's').
`AudioChannelPanel.java` is a `JPanel`.
And the prompt says: "Task 1: Locate and rewrite the pure Swing leaf component AudioChannelsPanel. Convert it entirely right now using native JavaFX layouts and controls.
Task 2: Wrap this newly created JavaFX AudioChannelsPanel in a javax.swing.JFXPanel so it can continue to be hosted inside its legacy Swing parent container.
Task 3: Refactor the event handling to enforce the MVC pattern. Remove all Swing ActionListeners and replace them with JavaFX Properties and Data Binding, ensuring complete separation of the UI from the audio and radio logic."

But `AudioChannelsPanel` is ALREADY a `JFXPanel` wrapper! Wait, look at the code for `AudioChannelsPanel.java`:
```java
public class AudioChannelsPanel extends JFXPanel
```
And it loads `AudioChannelsPanel.fxml`.
Wait, look at `AudioChannelPanel.java` (singular). It is a `JPanel`.
Is it possible the prompt meant `AudioChannelPanel`? "Locate and rewrite the pure Swing leaf component AudioChannelsPanel." Wait, it could be a typo in the prompt, or maybe I need to rewrite `AudioChannelPanel` (singular) and rename it/wrap it?
If the prompt says `AudioChannelsPanel` but it's already converted to a `JFXPanel`, wait... What if I need to rewrite `AudioChannelPanel` and wrap IT in a `JFXPanel`, OR the prompt made a typo and meant `AudioChannelPanel`? Let's check where `AudioChannelPanel` is created: `AudioChannelsPanelController.java`.
In `AudioChannelsPanelController.java`:
```java
AudioChannelPanel swingPanel = new AudioChannelPanel(...);
SwingNode swingNode = new SwingNode();
SwingUtilities.invokeLater(() -> swingNode.setContent(swingPanel));
```
Aha! `AudioChannelsPanelController` embeds `AudioChannelPanel` (which is Swing) inside a `SwingNode`!
So the pure Swing leaf component is `AudioChannelPanel`!
But the prompt says: "Locate and rewrite the pure Swing leaf component AudioChannelsPanel." and "Wrap this newly created JavaFX AudioChannelsPanel in a javax.swing.JFXPanel...".
This is weird, because if I wrap `AudioChannelPanel` in a `JFXPanel`, then I'll have `AudioChannelsPanel` (JFXPanel) -> `AudioChannelsPanelController` (JavaFX) -> `AudioChannelPanel` (JFXPanel) which is weird since the parent is already JavaFX!
Actually, wait... if I rewrite `AudioChannelPanel` to JavaFX, I don't need `JFXPanel` for it, except maybe it's used elsewhere? Let's check where `AudioChannelPanel` is used.
