All code review feedback addressed:
- Filter tree checkboxes recursion fixed.
- `EventClearButton` uses `Slider` directly instead of a `TextInputDialog`, keeping the UI regression away.
- Missing `ClearableHistoryModel` reference replaced properly by restoring original structure.
