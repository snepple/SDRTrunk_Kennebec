Fix flatlaf window decorations by setting properties early

- Fix flatlaf.useWindowDecorations and flatlaf.window.backdrop being
  applied after the look and feel was already set.
- Also initialize the ThemeManager before any JFrame instances are
  created to ensure that JFrames use the LookAndFeel's decorations.
