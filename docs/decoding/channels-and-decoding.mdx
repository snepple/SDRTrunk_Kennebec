---
title: "Channels & Decoding in SDRTrunk Kennebec"
description: "Add and configure channels in the SDRTrunk Kennebec Playlist Editor — set protocol, frequency, and source, then manage channel states in the Now Playing view."
sidebarTitle: "Channels"
---

A channel in SDRTrunk Kennebec represents a single radio signal you want to monitor. It ties together three things: a receive frequency, a decoding protocol, and an alias list that translates raw IDs into meaningful names. Every channel lives inside a playlist, and you manage channels through the **Playlist Editor**.

## Control channels and traffic channels

On trunked radio systems, two distinct channel roles exist:

- **Control channel** — a continuously transmitted data stream that carries system signaling: call grants, frequency assignments, and site information. You configure a control channel explicitly in the **Playlist Editor**. SDRTrunk Kennebec listens to this channel to learn where voice calls will appear.
- **Traffic channel** — a temporary voice channel assigned by the system controller for the duration of a call. SDRTrunk Kennebec follows traffic channel grants automatically using the information decoded from the control channel. You do not configure traffic channels individually.

On conventional (non-trunked) systems, every channel is a fixed frequency with no separate control channel.

## Add and configure a channel

<Steps>
  <Step title="Open the Playlist Editor">
    Select **View → Playlist Editor** from the main menu. The editor opens with a sidebar showing **Playlists**, **Channels**, **Aliases**, **Streaming**, **Radio Reference**, and **Two Tones**.
  </Step>
  <Step title="Go to the Channels section">
    Click **Channels** in the sidebar. The upper pane displays a table of every channel in the active playlist.
  </Step>
  <Step title="Create a new channel">
    Click **New** in the top-right toolbar. A dropdown lists the supported decoder types. Select the protocol that matches your system — for example, **P25 Phase 1**, **DMR**, or **NBFM**. A new channel row appears and the detail editor opens below.
  </Step>
  <Step title="Set the channel fields">
    Fill in the required fields in the detail editor:

    | Field | Description |
    | --- | --- |
    | **Name** | A short label such as `Fire Dispatch` or `PD Car-to-Car`. Appears in the Now Playing panel and in recorded audio filenames. |
    | **System** | Free-text grouping label for the radio network, for example `County EMS`. |
    | **Site** | The tower or repeater name within the system. Leave blank for single-site conventional channels. |
    | **Frequency** | Receive frequency in Hz. For trunked systems, enter the control channel frequency. |
    | **Alias List** | The alias list used to resolve talkgroup and radio IDs decoded on this channel. |
  </Step>
  <Step title="Configure protocol-specific options">
    Each decoder type adds its own settings below the common fields — traffic channel pool size, NAC filtering for P25, color code for DMR, squelch thresholds for NBFM, and so on. See the protocol-specific pages for details.
  </Step>
  <Step title="Save the channel">
    Click **Save**. The channel row updates immediately in the Channels table.
  </Step>
</Steps>

## Enable and disable channels

You can start and stop a channel at any time without deleting it:

- **Start decoding** — select the channel row and click the **Play** button in the toolbar, or double-click the row.
- **Stop decoding** — select a running channel and click **Stop**, or double-click the row.
- **Auto-Start** — enable the **Auto-Start** checkbox in the channel detail editor to have SDRTrunk Kennebec start this channel automatically every time the application launches.

<Warning>
Enabling **Auto-Start** on a large number of channels simultaneously increases CPU and SDR tuner load at startup. Enable it selectively for the channels you monitor most frequently.
</Warning>

## Channel states in the Now Playing view

The **Now Playing** panel in the main window shows all channels that are currently active. Each row reflects the channel's live state:

| State | Meaning |
| --- | --- |
| **Idle** | The channel is running and locked to the control channel, but no voice call is active. |
| **Call** | A voice grant is active; the decoder is following a traffic channel. |
| **No Tuner** | The channel is configured but no tuner is available to cover the frequency. |
| **Muted** | Audio is suppressed, either manually or because the **Ignore Unaliased Talkgroups** option is active. |

## Filter and sort the Channels table

The **All / Playing / Auto-Start** segmented control at the top of the Channels section narrows the table to all channels, only those currently decoding, or only those with Auto-Start enabled. The **Search** field filters by system, site, name, or protocol as you type. Click any column header to sort; click again to reverse the order.

## Supported protocols

SDRTrunk Kennebec includes a decoder for each of the following protocols. Select the appropriate type from the **New** dropdown when creating a channel.

<CardGroup cols={2}>
  <Card title="P25 Phase 1 & Phase 2" icon="radio" href="/docs/decoding/p25">
    APCO-25 digital trunked and conventional systems used by North American public safety agencies. Supports C4FM, LSM/CQPSK, and HDQPSK (Phase 2 TDMA).
  </Card>
  <Card title="DMR" icon="signal" href="/docs/decoding/dmr">
    Digital Mobile Radio — commercial fleet and utility systems in Tier I, Tier II, and Tier III (trunked) variants, including MOTOTRBO networks.
  </Card>
  <Card title="NBFM & AM" icon="wave-square" href="/docs/decoding/analog">
    Narrowband FM and AM conventional channels with configurable squelch, CTCSS/DCS tone filtering, and post-demodulation audio filters.
  </Card>
  <Card title="LTR Standard" icon="tower-broadcast">
    E.F. Johnson Logic Trunked Radio — an analog trunking protocol that embeds control signaling in-band on each channel. Select **LTR Standard** from the decoder dropdown when adding a channel.
  </Card>
  <Card title="LTR-Net" icon="tower-broadcast">
    An enhanced version of LTR that uses a dedicated control channel for faster call setup. Select **LTR-Net** when adding a channel.
  </Card>
  <Card title="MPT1327" icon="tower-broadcast">
    A European analog trunked radio standard widely deployed on commercial and utility systems. Select **MPT-1327** when adding a channel.
  </Card>
</CardGroup>

<Note>
The **Passport** decoder type is also available for Motorola Passport analog trunking systems.
</Note>
