## 2026-05-17 - Migrate JOptionPane to JavaFX Alert\n**Finding:** Legacy Swing dialogs (JOptionPane) were used across various classes, violating HIG and JavaFX modernization.\n**Action:** Replaced all usages of `javax.swing.JOptionPane` with `javafx.scene.control.Alert` running inside `Platform.runLater()` to ensure thread safety. Converted string concatenations for message blocks. Also bumped project version.
